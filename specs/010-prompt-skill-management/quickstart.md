# Quickstart: 配置驱动 Agent

1. 打开设置中的“AI 配置中心”，选择“对话路由 / 文章查询”。
2. 创建 Skill 版本，设置默认参数 `limit: 10`，保存后先试运行“最近文章”。
3. 查看结构化工具调用及结果；确认没有业务写入后启用该版本。
4. 在自助 Agent 发送“最近文章”，应直接显示 10 篇可点击文章卡片。
5. 若结果不符合预期，从版本历史回退，并检查运行记录中的版本绑定。
