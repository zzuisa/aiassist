import { describe, expect, it } from 'vitest'

// Placeholder proving the Vitest harness runs; real suites arrive per user story.
describe('frontend test harness', () => {
  it('is configured', () => {
    expect(1 + 1).toBe(2)
  })
})
